! function() {
    "use strict";
    const e = (e, t = !1) => (e = e.trim(), t ? [...document.querySelectorAll(e)] : document.querySelector(e)),
        t = (t, i, s, o = !1) => {
            let a = e(i, o);
            a && (o ? a.forEach(e => e.addEventListener(t, s)) : a.addEventListener(t, s))
        },
        i = e => {
            window.scrollTo({
                top: 0,
                behavior: "smooth"
            })
        };
    t("click", ".mobile-nav-toggle", function(t) {
        e("#navbar").classList.toggle("navbar-mobile"), this.classList.toggle("bi-list"), this.classList.toggle("bi-x")
    }), t("click", "#navbar .nav-link", function(t) {
        let s = e(this.hash);
        if (s) {
            t.preventDefault();
            let o = e("#navbar"),
                a = e("#header"),
                l = e("section", !0);
            if (e("#navbar .nav-link", !0).forEach(e => {
                    e.classList.remove("active")
                }), this.classList.add("active"), o.classList.contains("navbar-mobile")) {
                o.classList.remove("navbar-mobile");
                let t = e(".mobile-nav-toggle");
                t.classList.toggle("bi-list"), t.classList.toggle("bi-x")
            }
            if ("#header" == this.hash) return a.classList.remove("header-top"), void l.forEach(e => {
                e.classList.remove("section-show")
            });
            a.classList.contains("header-top") ? (l.forEach(e => {
                e.classList.remove("section-show")
            }), s.classList.add("section-show")) : (a.classList.add("header-top"), setTimeout(function() {
                l.forEach(e => {
                    e.classList.remove("section-show")
                }), s.classList.add("section-show")
            }, 350)), i(this.hash)
        }

    }, !0), window.addEventListener("load", () => {
        if (window.location.hash) {
            let t = e(window.location.hash);
            if (t) {
                let s = e("#header"),
                    o = e("#navbar .nav-link", !0);
                s.classList.add("header-top"), o.forEach(e => {
                    e.getAttribute("href") == window.location.hash ? e.classList.add("active") : e.classList.remove("active")
                }), setTimeout(function() {
                    t.classList.add("section-show")
                }, 350), i(window.location.hash)
            }
        }
    });
    let s = e(".skills-content");
    s && new Waypoint({
        element: s,
        offset: "80%",
        handler: function(t) {
            e(".progress .progress-bar", !0).forEach(e => {
                e.style.width = e.getAttribute("aria-valuenow") + "%"
            })
        }
    }), new Swiper(".certificates-slider", {
        speed: 600,
        loop: !0,
        autoplay: {
            delay: 5e3,
            disableOnInteraction: !1
        },
        slidesPerView: "auto",
        pagination: {
            el: ".swiper-pagination",
            type: "bullets",
            clickable: !0
        },
        breakpoints: {
            320: {
                slidesPerView: 1,
                spaceBetween: 20
            },
            1200: {
                slidesPerView: 3,
                spaceBetween: 20
            }
        }
    }), window.addEventListener("load", () => {
        let i = e(".portfolio-container");
        if (i) {
            let s = new Isotope(i, {
                    itemSelector: ".portfolio-item",
                    layoutMode: "fitRows"
                }),
                o = e("#portfolio-flters li", !0);
            t("click", "#portfolio-flters li", function(e) {
                e.preventDefault(), o.forEach(function(e) {
                    e.classList.remove("filter-active")
                }), this.classList.add("filter-active"), s.arrange({
                    filter: this.getAttribute("data-filter")
                })
            }, !0)
        }
    });
    GLightbox({
        selector: ".portfolio-lightbox"
    }), GLightbox({
        selector: ".portfolio-details-lightbox",
        width: "90%",
        height: "90vh"
    });

    GLightbox({
        selector: ".portfolio-lightbox"
    }), GLightbox({
        selector: ".certs-details-lightbox",
        width: "90%",
        height: "90vh"
    });

    new Swiper(".portfolio-details-slider", {
        speed: 400,
        loop: !0,
        autoplay: {
            delay: 5e3,
            disableOnInteraction: !1
        },
        pagination: {
            el: ".swiper-pagination",
            type: "bullets",
            clickable: !0
        }
    })

    new Swiper(".certs-details-slider", {
        speed: 400,
        loop: !0,
        autoplay: {
            delay: 5e3,
            disableOnInteraction: !1
        },
        pagination: {
            el: ".swiper-pagination-certs",
            type: "bullets",
            clickable: !0
        }
    })

    
}();

// function calcTime(evt, year) {
//     var date = new Date();
//     var year = date.getFullYear();

//     evt.currentTarget.innerHTML = ("Name " + year);

// }


function openCity(evt, cityName) {
    // Declare all variables
    var i, tabcontent, tablinks;
  
    // Get all elements with class="tabcontent" and hide them
    tabcontent = document.getElementsByClassName("tabcontent");
    for (i = 0; i < tabcontent.length; i++) {
      tabcontent[i].style.display = "none";
    }
  
    // Get all elements with class="tablinks" and remove the class "active"
    tablinks = document.getElementsByClassName("tablinks");
    for (i = 0; i < tablinks.length; i++) {
      tablinks[i].className = tablinks[i].className.replace(" active", "");
    }
  
    // Show the current tab, and add an "active" class to the link that opened the tab
    document.getElementById(cityName).style.display = "block";
    evt.currentTarget.className += " active";
  }

  function openPanel(evt, cityName, catg) {
    // Declare all variables
    var i, tabcontent, tablinks;
  
    // Get all elements with class="tabcontent" and hide them
    classame = catg + "__StyledTabPanel-jxdnBi bhnhgd"
    tabcontent = document.getElementsByClassName(classame);
    for (i = 0; i < tabcontent.length; i++) {
      tabcontent[i].style.display = "none";
    }
  
    // Get all elements with class="tablinks" and remove the class "active"
    classame = catg + "__StyledTabButton-hvALES fIjQyg"
    tablinks = document.getElementsByClassName(classame);
    for (i = 0; i < tablinks.length; i++) {
      tablinks[i].className = tablinks[i].className.replace(" active", "");
    }
  
    // Show the current tab, and add an "active" class to the link that opened the tab
    document.getElementById(cityName).style.display = "block";
    evt.currentTarget.className += " active";
  }

  




const $ = document.querySelector.bind(document);
const $$ = document.querySelectorAll.bind(document);

const tabs = $$(".tab-item");
const panes = $$(".tab-pane");

const tabActive = $(".tab-item.active");
const line = $(".tabs .line");

// SonDN fixed - Active size wrong size on first load.
// Original post: https://www.facebook.com/groups/649972919142215/?multi_permalinks=1175881616551340
requestIdleCallback(function () {
  line.style.left = tabActive.offsetLeft + "px";
  line.style.width = tabActive.offsetWidth + "px";
});

tabs.forEach((tab, index) => {
  const pane = panes[index];

  tab.onclick = function () {
    $(".tab-item.active").classList.remove("active");
    $(".tab-pane.active").classList.remove("active");

    line.style.left = this.offsetLeft + "px";
    line.style.width = this.offsetWidth + "px";

    this.classList.add("active");
    pane.classList.add("active");
  };
});


// test
// Function to get the current date and time
function getCurrentDateAndTime() {
    const dateTime = new Date();
    return dateTime.toLocaleString();
  }
  
  // Target an HTML element to display the current date and time
  const dateDisplay = document.getElementById("date-today");
  
  // Set the innerHTML of the element to the current date 
  dateDisplay.innerHTML = getCurrentDateAndTime();

function sectionZ() {
    var sections = document.getElementsByTagName('section');
    for(var i = 0; i < sections.length; ++i) {  
        sections[i].style.zIndex = "0";
        console.log("OIE");
    }
}
  
function showClapper(){

    var index = $("a.nav-link.active").hash;
    console.log(index);

    if(index != "#header"){
        document.querySelector('.clapper').style.display = "none";
        document.querySelector('.creditsHome').style.display = "none";
        window.location.hash = index;
    }else{
        document.querySelector('.clapper').style.display = "block";
        document.querySelector('.creditsHome').style.display = "block";
        window.location.hash = index;
        sectionZ();
    }
    
}



  // Run the function on load, if any elements are already visible without scrolling
//   window.addEventListener("scroll", checkFloatingDiv);
function goToPT() {
    // Declare all variables
    console.log("olar");
    var index = $("a.nav-link.active").hash;

    if(index != "#header"){
        window.location.href = "pt.html" + index;

    }else{
        window.location.href = "pt.html";
    }


  }

function goToEN() {
    // Declare all variables
    console.log("olar");
    var index = $("a.nav-link.active").hash;

    if(index != "#header"){
    window.location.href = "index.html" + index;
    }else{
        window.location.href = "index.html";
    }


  }


window.onload = function() {
    sectionZ();
    setTimeout(function(){
        showClapper();}, 150);
};

// $(document).ready(function(){
//     console.log("OLARRRRRRR");
//     showClapper();}
// )